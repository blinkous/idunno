<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.idunno">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".door2" />
        <activity android:name=".startAgain" />
        <activity android:name=".guess1" />
        <activity android:name=".guess2" />
        <activity android:name=".guess3" />
        <activity android:name=".myCanvas" />
        <activity android:name=".ColorPickerDialog" />
        <activity android:name=".myColor"></activity>
    </application>

</manifest>