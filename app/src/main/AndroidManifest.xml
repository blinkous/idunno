<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.idunno">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/DarkTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".door2" />
        <activity android:name=".door3" />
        <activity android:name=".guess1" />
        <activity android:name=".guess2" />
        <activity android:name=".guess3" />
        <activity
            android:name=".myCanvas"
            android:screenOrientation="portrait" />
        <activity android:name=".door1" />
        <activity android:name=".testing" />
        <activity android:name=".myList" />
        <activity android:name=".TestingGradients" />
        <activity android:name=".myCanvasViewStub" />
        <activity android:name=".RecyclerViewTest"></activity>
    </application>

</manifest>